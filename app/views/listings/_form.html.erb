<%= form_for(@listing) do |f| %>
  <% if @listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@listing.errors.count, "error") %> prohibited this listing from being saved:</h2>

      <ul>
      <% @listing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :club_id %><br>
    <%= f.collection_select :club_id, @clubs, :id, :club%>
  </div>
  <div class="field">
    <%= f.label :sport_id %><br>
    <%= f.collection_select :sport_id, @sports, :id, :sport%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
